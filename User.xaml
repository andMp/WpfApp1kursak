<Page x:Class="WpfApp1kursak.User"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
      Title="User">

    <Page.Resources>
        <!-- Стиль кнопок -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF4472C4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Page.Resources>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock x:Name="id" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="18" FontWeight="Bold" Foreground="#333"/>

        <!-- Ліва колонка -->
        <Border Grid.Row="1" Grid.Column="0" Margin="10" Padding="10" Background="White" CornerRadius="10" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Мої дані:"
                           FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Телефон:" VerticalAlignment="Center"/>
                <TextBox x:Name="telBox" Grid.Row="1" Grid.Column="1" Margin="5" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Пароль:" VerticalAlignment="Center"/>
                <TextBox x:Name="parBox" Grid.Row="2" Grid.Column="1" Margin="5" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Посада:" VerticalAlignment="Center"/>
                <TextBlock x:Name="posadaBox" Grid.Row="3" Grid.Column="1" Margin="5" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Рівень доступу:" VerticalAlignment="Center"/>
                <TextBlock x:Name="rivDostBox" Grid.Row="4" Grid.Column="1" Margin="5" />

                <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Content="Зберегти зміни" Click="SaveChanges_Click"/>
            </Grid>
        </Border>

        <!-- Центральна колонка -->
        <Border Grid.Row="1" Grid.Column="1" Margin="10" Padding="10" Background="White" CornerRadius="10">
            <Grid x:Name="middleColumn">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Список опитувань:" FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Visible">
                    <ListBox x:Name="OpitList" PreviewMouseWheel="UserList_PreviewMouseWheel">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" Margin="5">
                                    <TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Tema}" />
                                    <TextBlock FontSize="12">
                                        <Run Text="Питань: "/>
                                        <Run Text="{Binding KstPitann, Mode=OneWay}" />
                                        <Run Text="  Тривалість: "/>
                                        <Run Text="{Binding TrivOp, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
                <Button x:Name="startOp" Grid.Row="2" Content="Розпочати опитування!" Click="goToOpituvClick"/>
            </Grid>
        </Border>

        <!-- Права колонка -->
        <Border Grid.Row="1" Grid.Column="2" Margin="10" Padding="10" Background="White" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Проходження опитування:"
                           FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Тема:"/>
                <TextBlock x:Name="temaOp" Grid.Row="1" Grid.Column="1"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Залишилось часу:"/>
                <TextBlock x:Name="zalChas" Grid.Row="2" Grid.Column="1"/>

                <TextBlock x:Name="nomPit" Grid.Row="3" Grid.Column="0"/>
                <DockPanel Grid.Column="1" Grid.Row="3">
                    <TextBlock x:Name="pit" DockPanel.Dock="Left" Width="76"/>
                    <Grid DockPanel.Dock="Right">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Button Content="˄" Grid.Row="0" Click="PrevQuestion_Click"/>
                        <Button Content="˅" Grid.Row="1" Click="NextQuestion_Click"/>
                    </Grid>
                </DockPanel>

                <TextBlock x:Name="vidpNaPit" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap" />

                <Button x:Name="yes" Grid.Row="5" Grid.Column="0" Content="Так" Click="yes_Click"/>
                <Button x:Name="no" Grid.Row="5" Grid.Column="1" Content="Ні" Click="no_Click"/>

                <Button x:Name="endGol" Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                        Content="Завершити голосування" Click="endGol_Click"/>
            </Grid>
        </Border>
    </Grid>
</Page>
